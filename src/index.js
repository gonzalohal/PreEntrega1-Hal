import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Asegúrate de crear este archivo después

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
